name: form.block.fromfields.checkbox
icon: check

fields:
  options:
    label: form.block.fromfields.checkbox.options
    type: structure
    required: true
    columns:
      label:
        type: text
      selected:
        type: bool

    fields:
      label:
        label: form.block.fromfields.checkbox.options.label
        type: textarea
        required: true
        width: 1

      slug:
        label: form.block.fromfields.checkbox.options.slug
        type: slug
        wizard:
          text: " "
          field: label
        required: true
        width: 1/2

      selected:
        label: form.block.fromfields.checkbox.options.selected
        type: toggle
        width: 1/2

