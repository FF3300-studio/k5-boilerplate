label: Persone e responsabilità
width: 1/2
type: structure
whenQuery: collection_toggle = false && parent_collection_options != 'map' &&  parent_collection_options != NULL 
fields:
  persona:
    label: Scegli una persona
    type: select
    options: query
    query:
      fetch: site.persone.toStructure
      text: "{{ structureItem.nome }}"
      value: "{{ structureItem.nome }}"
  ruolo:
    label: Indica il ruolo o la mansione
    type: text